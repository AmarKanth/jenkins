apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm_deployment.fullname" . }}
  labels:
    {{- include "helm_deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "helm_deployment.selectorLabels" . | nindent 4 }}
